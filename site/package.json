{
	"name": "dictawhisper-site",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "node ../scripts/ensure-lease.mjs dictawhisper-site 5186 && node ../scripts/filepress-dev-lease.mjs dev",
		"build": "filepress build",
		"preview": "filepress preview",
		"check": "filepress check",
		"ship": "pnpm build && wrangler pages deploy build --project-name=dictawhisper",
		"deploy": "pnpm ship"
	},
	"devDependencies": {
		"getfilepress": "^0.1.3",
		"wrangler": "^4.28.0"
	},
	"pnpm": {
		"onlyBuiltDependencies": ["esbuild", "getfilepress", "workerd"]
	}
}
